pipeline {
    agent any
    
    stages {
        stage('Hello') {
            steps {
                echo '🎉 Jenkins CI/CD is working!'
                echo "Build Number: ${env.BUILD_NUMBER}"
            }
        }
    }
}
